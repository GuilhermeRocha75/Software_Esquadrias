[project]
name = "esquadrias-engine"
version = "0.4.0"
description = "Primeiro protótipo do motor técnico CR para esquadrias de correr"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
