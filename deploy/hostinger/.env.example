# Copy to deploy/hostinger/.env on the VPS. Do not commit real credentials.
WEB_PORT=8080
# Same-origin /api does not require CORS; set this only for a separate frontend origin.
CORS_ALLOWED_ORIGINS=
